module.exports = {
  tabWidth: 4,              // Use 4 spaces for indentation (matches ESLint config)
  useTabs: false,           // Use spaces instead of tabs
  endOfLine: 'lf',          // Use Unix line endings for cross-platform compatibility
  printWidth: 80,           // Wrap lines at 80 characters for readability
  singleQuote: true,        // Use single quotes for strings (matches ESLint)
  trailingComma: 'es5',     // Add trailing commas where valid in ES5 (objects, arrays)
  bracketSpacing: true,     // Add spaces inside object literals { foo: bar }
  arrowParens: 'always',    // Always wrap arrow function parameters in parentheses
};
